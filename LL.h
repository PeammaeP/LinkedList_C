class LL{
   private: //private by default if it's invisible
      NODE* hol; //head of linked list 
      int size;
   public:
       void add_node(NODE*&);
       void show_all();
      ~LL();
       LL();
      
};
